
import ArtistLayout from "@/Layouts/ArtistLayout"

const Activities = (props) => {
  return (
    <ArtistLayout auth={props.auth} >
      <section>
      <div>Activities</div>
      </section>
    </ArtistLayout>
    
  )
}

export default Activities